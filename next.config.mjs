/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: ['rickandmortyapi.com', 'utfs.io'], // Añade el dominio rickandmortyapi.com aquí
    },
};

export default nextConfig;
