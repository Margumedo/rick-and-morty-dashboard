// Definir el tipo de Character basado en la respuesta de la API
export type lastCharactersTables = {
    id: number;
    name: string;
    status: string;
    species: string;
    type: string;
    gender: string;
    image: string;
};